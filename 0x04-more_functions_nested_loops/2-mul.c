#include "main.h"

/**
 * mul - Entry point
 * Description: multiply two numbers
 * @a: one of the numbers to multiply
 * @b: another one of the numbers
 * Return: product
 */

int mul(int a, int b)
{
	int product;

	product = a * b;
	return (product);
}
