#include "main.h"

/**
 * _strpbrk - locate the first occurrence in a string
 * @s: string to string to be scanned
 * @accept: containing the characters to match
 *
 * Return: a pointer to the first character
 */

char *_strpbrk(char *s, char *accept)
{
}
