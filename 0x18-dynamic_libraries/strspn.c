#include "main.h"

/**
 * _strspn - calculate the length of a string in another
 * @s: string to be scanned
 * @accept: string containing the character
 *
 * Return: length of substring found
 */

unsigned int _strspn(char *s, char *accept)
{
}
